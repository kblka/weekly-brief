# Calendar IDs to include (comma-separated). Use "primary" for main calendar.
# Run with --list-calendars to see available IDs.
CALENDAR_IDS=primary

# Timezone for event display (e.g. America/New_York, Europe/London)
TIMEZONE=America/New_York

# RSS feed base URL (for private podcast delivery).
# If using GitHub Pages: https://yourusername.github.io/weekly-brief/
# Leave empty to skip RSS generation and use manual Spotify for Podcasters upload.
RSS_BASE_URL=

# Google Cloud: TTS uses same credentials as Calendar if GOOGLE_APPLICATION_CREDENTIALS
# is not set. For separate TTS service account, set path to JSON key:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/tts-service-account.json
